import { strictEqual as eq } from 'assert';
import { before, describe, it } from 'zunit';
import Database from '../src/Database';

export default describe('Database', () => {

  it('should connect', () => {
    
  });
});